Module 11 — Slice 5 Execution Plan
Custom Groups: schema uplift

This is a plan only. It was prepared from the current working tree and applies R-CG-01, R-CG-03, R-CG-04, and R-CG-05 from Docs/Business Logic of Module 11.md. It does not change the group create/read/rename/delete commands, the visit-composer handler, the Presentation layer, or any test beyond what is needed for schema verification.

Verified starting state
Area / artifact	Current verified state	Slice 5 action
TestGroup entity	Exists at src/MasrLab.Domain/Entities/Core/TestGroup.cs. Has GroupName (string), GroupPrice (decimal, required), and ICollection<TestGroupItem> TestGroupItems.	Remove GroupPrice. Add [NotMapped] decimal TotalGroupPrice computed property (sum of non-deleted items' Price).
TestGroupItem entity	Exists at src/MasrLab.Domain/Entities/Core/TestGroupItem.cs. Has TestGroupId (int), TestId (int), DisplayOrder (int). No Price property. No navigation properties.	Add decimal Price (required). No navigation properties added.
TestGroupConfiguration	Exists. Maps GroupName (nvarchar(200), required) and GroupPrice (decimal(18,2), required). Indexes on GroupName and IsDeleted. No explicit HasMany/WithOne relationship.	Remove GroupPrice configuration. The HasMany/WithOne is already inferred by convention and picked up by the snapshot (line 2925–2929); the explicit FK is added via the migration.
TestGroupItemConfiguration	Exists. Maps TestGroupId, TestId, DisplayOrder (all required). Indexes on each plus IsDeleted. No FK constraints, no composite unique index.	Add Price (decimal(18,2), required). Add filtered unique index (TestGroupId, TestId) where IsDeleted = 0. Add explicit FKs: TestGroupId → TestGroup.Id (cascade) and TestId → Test.Id (restrict).
GroupPrice usage	Declared in entity (line 8 of TestGroup.cs) and configured in EF (line 16 of TestGroupConfiguration.cs). Never read, written, or referenced in any Application handler, DTO, test, ViewModel, or XAML. The ManageTestGroupsCommandHandler only sets GroupName. All other matches are migration snapshots or documentation.	Safe to remove.
TestGroupItemDto	Exists at src/MasrLab.Application/Common/DTOs/TestGroupItemDto.cs. Has Id, TestGroupId, TestId — no Price.	Add decimal Price property. (Slice 6 will consume it; adding it here keeps the DTO in sync with the schema.)
TestGroupDto	Exists at src/MasrLab.Application/Common/DTOs/TestGroupDto.cs. Has Id, GroupName — no TotalGroupPrice.	No change in this slice (Slice 6 will add it when the query layer is updated).
ManageTestGroupsCommand	Accepts (int? Id, string GroupName, string TestIds). Creates items with TestGroupId, TestId, DisplayOrder only — no price.	Do not modify. Slice 6 will update this to accept per-test prices.
AddTestsToVisitCommandHandler	ResolveSelectionGroupTestsAsync returns only TestId list from group items (line 161). Does not read item prices.	Do not modify. Slice 6 will update this to use group-item prices.
DbContext / DI	DbSet<TestGroup> and DbSet<TestGroupItem> exist. ITestGroupItemRepository is registered.	No change.
Model snapshot (line 2923–2929)	TestGroupItem has one FK: TestGroupId → TestGroup (cascade). No TestId → Tests FK. No composite unique index.	Modify as shown below.
Model snapshot (line 1243–1281)	TestGroup has GroupPrice property. No TotalGroupPrice.	Remove GroupPrice. Add TotalGroupPrice (not mapped).
Migrations	Latest migration is 20260820120000_Slice4PriceListItemUniquenessAndForeignKeys.	Create Slice5CustomGroupsSchemaUplift (timestamp generated by dotnet ef migrations add; then replace Up method with guarded version).
Existing FK chain	FK_TestGroupItems_TestGroups_TestGroupId exists in DB (from InitialCreate). FK_TestGroupItems_Tests_TestId does NOT exist.	The migration must add FK_TestGroupItems_Tests_TestId (restrict).
Discrepancies found vs. Docs/Business Logic of Module 11.md
None. The Slice 5 scope in the business logic document aligns with the current codebase state. The plan below handles everything specified.

Exact production changes
src/MasrLab.Domain/Entities/Core/TestGroup.cs (replace entire file)
using System.ComponentModel.DataAnnotations.Schema;
using MasrLab.Domain.Common;

namespace MasrLab.Domain.Entities.Core;

public class TestGroup : BaseEntity
{
    public string GroupName { get; set; } = string.Empty;

    [NotMapped]
    public decimal TotalGroupPrice =>
        TestGroupItems.Where(i => !i.IsDeleted).Sum(i => i.Price);

    public ICollection<TestGroupItem> TestGroupItems { get; set; } = new List<TestGroupItem>();
}
Notes:

GroupPrice is removed. It was unreferenced in all Application, Infrastructure, Presentation, and test code.
TotalGroupPrice is [NotMapped] and computed from the navigation collection. EF Core will ignore it for schema purposes; the domain/project-layer sum is available for DTOs and display.
The TestGroupItems collection navigation is retained (it already exists and is used by the snapshot/convention FK).
src/MasrLab.Domain/Entities/Core/TestGroupItem.cs (replace entire file)
using MasrLab.Domain.Common;

namespace MasrLab.Domain.Entities.Core;

public class TestGroupItem : BaseEntity
{
    public int TestGroupId { get; set; }
    public int TestId { get; set; }
    public decimal Price { get; set; }
    public int DisplayOrder { get; set; }
}
Notes:

Price is added as decimal (non-nullable, required by EF convention). It represents the per-member price snapshot (R-CG-04).
No navigation properties are added (matching the existing convention for this entity).
src/MasrLab.Infrastructure/Persistence/Configurations/Core/TestGroupConfiguration.cs (replace entire file)
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;
using MasrLab.Domain.Entities.Core;

namespace MasrLab.Infrastructure.Persistence.Configurations.Core;

public class TestGroupConfiguration : IEntityTypeConfiguration<TestGroup>
{
    public void Configure(EntityTypeBuilder<TestGroup> builder)
    {
        builder.ToTable("TestGroups");
        builder.HasKey(e => e.Id);
        builder.Property(e => e.Id).ValueGeneratedOnAdd();

        builder.Property(e => e.GroupName).HasMaxLength(200).IsRequired();

        builder.HasIndex(e => e.GroupName);
        builder.HasIndex(e => e.IsDeleted);
    }
}
Notes:

GroupPrice property configuration is removed (column removed from DB).
TotalGroupPrice is [NotMapped] so requires no configuration.
No HasMany/WithOne added — the existing convention-inferred relationship continues to work (the snapshot already picks it up at lines 2925–2929).
src/MasrLab.Infrastructure/Persistence/Configurations/Core/TestGroupItemConfiguration.cs (replace entire file)
using MasrLab.Domain.Entities.Core;
using Microsoft.EntityFrameworkCore;
using Microsoft.EntityFrameworkCore.Metadata.Builders;

namespace MasrLab.Infrastructure.Persistence.Configurations.Core;

public class TestGroupItemConfiguration : IEntityTypeConfiguration<TestGroupItem>
{
    public void Configure(EntityTypeBuilder<TestGroupItem> builder)
    {
        builder.ToTable("TestGroupItems");
        builder.HasKey(e => e.Id);
        builder.Property(e => e.Id).ValueGeneratedOnAdd();

        builder.Property(e => e.TestGroupId).IsRequired();
        builder.Property(e => e.TestId).IsRequired();
        builder.Property(e => e.Price).HasColumnType("decimal(18,2)").IsRequired();
        builder.Property(e => e.DisplayOrder).IsRequired();

        builder.HasIndex(e => new { e.TestGroupId, e.TestId })
            .HasFilter("[IsDeleted] = 0")
            .IsUnique();

        builder.HasIndex(e => e.TestGroupId);
        builder.HasIndex(e => e.TestId);
        builder.HasIndex(e => e.IsDeleted);

        builder.HasOne<TestGroup>()
            .WithMany(e => e.TestGroupItems)
            .HasForeignKey(e => e.TestGroupId)
            .OnDelete(DeleteBehavior.Cascade)
            .IsRequired();

        builder.HasOne<Test>()
            .WithMany()
            .HasForeignKey(e => e.TestId)
            .OnDelete(DeleteBehavior.Restrict)
            .IsRequired();
    }
}
Notes:

Price column added as decimal(18,2), required.
Filtered unique index on (TestGroupId, TestId) where IsDeleted = 0 — prevents the same test appearing twice in a group, while soft-deleted rows do not conflict.
Explicit FK TestGroupId → TestGroup (cascade) — makes the existing convention-inferred relationship explicit.
Explicit FK TestId → Test (restrict) — prevents deleting a test that is referenced by any group item.
src/MasrLab.Application/Common/DTOs/TestGroupItemDto.cs (replace entire file)
namespace MasrLab.Application.Common.DTOs;

public record TestGroupItemDto
{
    public int Id { get; init; }
    public int TestGroupId { get; init; }
    public int TestId { get; init; }
    public decimal Price { get; init; }
}
Notes:

Price added. This keeps the DTO in sync with the schema change so that Slice 6 (incremental CRUD handlers) can populate it. No existing code breaks because no current code reads TestGroupItemDto.Price yet.
EF Core migration
Run dotnet ef migrations add Slice5CustomGroupsSchemaUplift from the Infrastructure project after applying the entity/configuration changes above. EF Core will produce a timestamp. Then:

Replace the generated Up method with the guarded version below.
Rename both files to use the plan's fixed timestamp (or retain the generated timestamp and update the [Migration] attribute accordingly).
The Designer file's BuildTargetModel body is generated by EF Core and contains the full post-change snapshot — it must not be hand-edited beyond the Up method replacement.
The guard follows the same RAISERROR pattern as Slice 4: it checks for duplicate (TestGroupId, TestId) pairs among non-deleted rows before creating the unique index, and aborts with severity 16 if found.

Exact Up method (replaces the generated one)
protected override void Up(MigrationBuilder migrationBuilder)
{
    migrationBuilder.Sql(@"
IF EXISTS
(
    SELECT 1
    FROM [TestGroupItems]
    WHERE [IsDeleted] = 0
    GROUP BY [TestGroupId], [TestId]
    HAVING COUNT(*) > 1
)
BEGIN
    RAISERROR(
        'Cannot create unique index IX_TestGroupItems_TestGroupId_TestId because duplicate TestGroupItems rows exist for the same TestGroupId and TestId.',
        16,
        1);
END");

    migrationBuilder.Sql("ALTER TABLE [TestGroupItems] ADD [Price] decimal(18,2) NOT NULL DEFAULT 0");

    migrationBuilder.Sql("ALTER TABLE [TestGroups] DROP COLUMN [GroupPrice]");

    migrationBuilder.CreateIndex(
        name: "IX_TestGroupItems_TestGroupId_TestId",
        table: "TestGroupItems",
        columns: new[] { "TestGroupId", "TestId" },
        unique: true,
        filter: "[IsDeleted] = 0");

    migrationBuilder.AddForeignKey(
        name: "FK_TestGroupItems_Tests_TestId",
        table: "TestGroupItems",
        column: "TestId",
        principalTable: "Tests",
        principalColumn: "Id",
        onDelete: ReferentialAction.Restrict);
}
Notes:

The duplicate guard runs first, before AddColumn and before CreateIndex. If duplicates exist among non-deleted rows, the migration aborts with SQL Server error severity 16 (same pattern as Slice 4).
AddColumn for Price uses DEFAULT 0 so existing rows get Price = 0. This is safe: no existing code reads this column yet; Slice 6 handlers will set it on add/edit.
DropColumn for GroupPrice removes the unused column. The guard does not need to check this column — it was never populated with meaningful data (no handler ever wrote to it).
CreateIndex creates the filtered unique index.
AddForeignKey adds the TestId → Tests restrictive FK.
Exact Down method
protected override void Down(MigrationBuilder migrationBuilder)
{
    migrationBuilder.DropForeignKey(
        name: "FK_TestGroupItems_Tests_TestId",
        table: "TestGroupItems");

    migrationBuilder.DropIndex(
        name: "IX_TestGroupItems_TestGroupId_TestId",
        table: "TestGroupItems");

    migrationBuilder.DropColumn(
        name: "Price",
        table: "TestGroupItems");

    migrationBuilder.AddColumn<decimal>(
        name: "GroupPrice",
        table: "TestGroups",
        type: "decimal(18,2)",
        nullable: false,
        defaultValue: 0m);
}
ModelSnapshot diff
The snapshot will be auto-updated by dotnet ef migrations add. The two target-model changes in the generated Designer/snapshot are:

In the TestGroupItem entity section, after the existing single-column indexes:

                    b.HasIndex("TestGroupId", "TestId")
                        .IsUnique()
                        .HasFilter("[IsDeleted] = 0");
In the TestGroupItem relationship section, after the existing TestGroup FK:

                    b.HasOne("MasrLab.Domain.Entities.Core.Test", null)
                        .WithMany()
                        .HasForeignKey("TestId")
                        .OnDelete(DeleteBehavior.Restrict)
                        .IsRequired();
In the TestGroup entity section:

Remove the GroupPrice property line.
The TotalGroupPrice property is [NotMapped] so it does NOT appear in the snapshot.
In the TestGroup entity section, after HasKey("Id"):

No new index needed for TotalGroupPrice (it is not mapped).

Exact tests
tests/MasrLab.Application.Tests/TestGroupSchemaUpliftTests.cs (NEW)
using MasrLab.Domain.Entities.Core;

namespace MasrLab.Application.Tests;

public class TestGroupSchemaUpliftTests
{
    [Fact]
    public void TotalGroupPrice_SumsNonDeletedItemPrices()
    {
        var group = new TestGroup { GroupName = "Checkup" };
        group.TestGroupItems.Add(new TestGroupItem { TestGroupId = 1, TestId = 1, Price = 50m, DisplayOrder = 1 });
        group.TestGroupItems.Add(new TestGroupItem { TestGroupId = 1, TestId = 2, Price = 30m, DisplayOrder = 2 });
        group.TestGroupItems.Add(new TestGroupItem { TestGroupId = 1, TestId = 3, Price = 10m, DisplayOrder = 3, IsDeleted = true });

        Assert.Equal(80m, group.TotalGroupPrice);
    }

    [Fact]
    public void TotalGroupPrice_ReturnsZeroForEmptyGroup()
    {
        var group = new TestGroup { GroupName = "Empty" };
        Assert.Equal(0m, group.TotalGroupPrice);
    }

    [Fact]
    public void TestGroupItem_Price_DefaultsToZero()
    {
        var item = new TestGroupItem { TestGroupId = 1, TestId = 1, DisplayOrder = 1 };
        Assert.Equal(0m, item.Price);
    }
}
tests/MasrLab.Infrastructure.Tests/TestGroupItemUniqueIndexIntegrationTests.cs (NEW)

using MasrLab.Domain.Entities.Core;
using Microsoft.Data.SqlClient;
using Microsoft.EntityFrameworkCore;

namespace MasrLab.Infrastructure.Tests;

[Collection("LocalDb")]
public class TestGroupItemUniqueIndexIntegrationTests
{
    [LocalDbFact]
    public async Task UniqueIndex_RejectsDuplicateTestWithinOneGroup_ButAllowsAnotherGroup()
    {
        var databaseName = LocalDbTestDatabase.NewDatabaseName("MasrLabDb_TestGroupItemUnique");
        try
        {
            await using (var setup = LocalDbTestDatabase.CreateMigratedContext(databaseName))
            {
                var test = new Test
                {
                    Name = "CBC", ReportName = "CBC", ReceiptName = "CBC",
                    Group = "Blood", TurnaroundTime = "1 day", Unit = "count"
                };
                var firstGroup = new TestGroup { GroupName = "Checkup A" };
                var secondGroup = new TestGroup { GroupName = "Checkup B" };
                setup.AddRange(test, firstGroup, secondGroup);
                await setup.SaveChangesAsync(CancellationToken.None);

                setup.TestGroupItems.Add(new TestGroupItem
                {
                    TestGroupId = firstGroup.Id, TestId = test.Id, Price = 50m, DisplayOrder = 1
                });
                setup.TestGroupItems.Add(new TestGroupItem
                {
                    TestGroupId = secondGroup.Id, TestId = test.Id, Price = 60m, DisplayOrder = 1
                });
                await setup.SaveChangesAsync(CancellationToken.None);
            }

            await using (var duplicate = LocalDbTestDatabase.CreateMigratedContext(databaseName))
            {
                var firstGroupId = await duplicate.TestGroups.Select(x => x.Id).FirstAsync();
                var testId = await duplicate.Tests.Select(x => x.Id).SingleAsync();
                duplicate.TestGroupItems.Add(new TestGroupItem
                {
                    TestGroupId = firstGroupId, TestId = testId, Price = 70m, DisplayOrder = 2
                });
                var exception = await Assert.ThrowsAsync<DbUpdateException>(
                    () => duplicate.SaveChangesAsync(CancellationToken.None));
                var sql = Assert.IsType<SqlException>(exception.GetBaseException());
                Assert.True(sql.Number is 2601 or 2627);
            }
        }
        finally
        {
            await using var cleanup = LocalDbTestDatabase.CreateContext(databaseName);
await cleanup.Database.EnsureDeletedAsync();
        }
    }
}
Complete file list
Create:

src/MasrLab.Infrastructure/Persistence/Migrations/<timestamp>_Slice5CustomGroupsSchemaUplift.cs
src/MasrLab.Infrastructure/Persistence/Migrations/<timestamp>_Slice5CustomGroupsSchemaUplift.Designer.cs
tests/MasrLab.Application.Tests/TestGroupSchemaUpliftTests.cs
tests/MasrLab.Infrastructure.Tests/TestGroupItemUniqueIndexIntegrationTests.cs
Modify (replace entire file): 5. src/MasrLab.Domain/Entities/Core/TestGroup.cs 6. src/MasrLab.Domain/Entities/Core/TestGroupItem.cs 7. src/MasrLab.Infrastructure/Persistence/Configurations/Core/TestGroupConfiguration.cs 8. src/MasrLab.Infrastructure/Persistence/Configurations/Core/TestGroupItemConfiguration.cs 9. src/MasrLab.Application/Common/DTOs/TestGroupItemDto.cs 10. src/MasrLab.Infrastructure/Persistence/Migrations/MasrLabDbContextModelSnapshot.cs (auto-updated by EF Core)

No other source or test file may be created or modified.

Completion gate
dotnet build MasrLab.sln — must be zero errors.
dotnet test MasrLab.sln — all unit tests must pass; LocalDb-based integration tests must pass where LocalDb is available.
On a newly created scratch LocalDb, execute the full migration chain with context.Database.Migrate(); the migration must apply cleanly.
Seed a duplicate (TestGroupId, TestId) pair among non-deleted rows at the prior migration, then attempt this migration. It must stop on the RAISERROR guard and leave the unique index absent.
Run the LocalDb integration test above: it must find the real migration index and receive SQL error 2601 or 2627 for the duplicate while retaining the allowed same-test/different-group row.
Run the TotalGroupPrice unit tests — confirm the computed property sums non-deleted items correctly and returns 0 for empty groups.
Out of scope for this slice
Updating ManageTestGroupsCommand / handler to accept and persist per-test prices (Slice 6).
Updating AddTestsToVisitCommandHandler.ResolveSelectionGroupTestsAsync to use group-item prices (Slice 6).
Adding TotalGroupPrice to TestGroupDto and the query layer (Slice 6).
Presentation layer changes for the Custom Groups window.
Any changes to CommercialPackageItem, PriceListItem, or other entities.
